# beri-uptime
External watch on https://studio.beri.global from GitHub Actions every 5 minutes: alerts by push (ntfy) when live is not HTTP 200 or when the served version differs from the `LIVE_SHA` repository variable (updated at each lock-in).
